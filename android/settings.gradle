//include(':edfa-pg-sdk')
//project(':edfa-pg-sdk').projectDir = new File(settingsDir,"../../edfa-pg-android-sdk/edfa-pg-sdk")
//print(new File(settingsDir,"../../edfa-pg-android-sdk/edfa-pg-sdk"))

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' }   
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }   
    }
}

rootProject.name = "com.flutterflow.fa1red"
include ':app'
